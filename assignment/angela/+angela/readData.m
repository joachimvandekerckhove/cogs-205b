function data = readData(fileName)
% -------------------------------------------------------------------------
% data = readData(fileName) takes as input a file name (STR) and outputs a
% variable containing bivariate data.
% -------------------------------------------------------------------------

data = readmatrix(fileName);

end
