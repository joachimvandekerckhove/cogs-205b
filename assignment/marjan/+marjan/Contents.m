function [] = Contents()
%getData downloads the file in the first argument and saves it with
% the name in the second argument
%readData reads the datafile using the filename and puts it in a variable
%report(data) reports the properties Of the dataset,including the size of the dataset, the mean and covariance of the bivariate normal distribution
%main() creates a .mfile that calls required functions
%test() tests the validity of the fuctions in the package
    
    help getData
    help readData
    help report
    help main
    help test
end